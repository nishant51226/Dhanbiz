/** Shown when `users.email` would duplicate an existing row (any account type). */
export const DUPLICATE_USER_EMAIL_MESSAGE =
  "This email is already in use. Please use a different email address.";
